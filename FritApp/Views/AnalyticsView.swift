import SwiftUI

struct AnalyticsView: View {
    var body: some View {
        NavigationStack {
            ScrollView {
                VStack(spacing: 20) {
                    // Analytics content
                    Text("Analytics coming soon")
                }
                .padding()
            }
            .navigationTitle("Analytics")
        }
    }
} 